## 1.mysql.exe 无法通过命令行访问

解决方案：加入环境变量

## 2.TS2786: 'FaMusic' cannot be used as a JSX component. 顽固的类型定义冲突（TS2786）（版本问题）

解决方案：使用@ts-ignore 注释 来忽略这些特定行上的类型检查。

## 3.静音属性muted每个视频卡片单独一份，需要统一变化

解决方案：我们将把 isMuted 状态和切换逻辑从 VideoCard 组件提升到其父组件 VideoFeed 中，并将其作为 Props 传递给所有 VideoCard。