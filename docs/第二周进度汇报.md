# 短视频管理系统 – 第二周进度报告

**日期：2025/11/27**
**开发者：Falsing （zxj）**

---

## 一、项目环境与基础结构

* 重新完成前端新项目初始化（Vite + React19 + TypeScript）
* 修复 React19 相关兼容问题，使全部组件在 React19 下稳定运行
* 完成旧项目组件迁移（RecommendFeed / VideoFeed / VideoPlayer）
* 完整迁移：

  * 样式文件
  * mock 数据
  * 入口文件路由结构
* 解决类型报错：FC、JSX 命名空间、路径问题等

---

## 二、后端服务搭建（Node.js + Express）

* 完成 Express 后端基础架构

  * 配置 CORS
  * JSON body 解析
  * `/uploads` 静态资源托管
* 建立 MySQL 连接池并成功连接
* 已创建数据库与数据表：

  * `user`
  * `video`
* 修复数据库连接权限错误（ER_ACCESS_DENIED_ERROR）
* 使用 bcrypt 完成密码加密
* 使用 JWT 完成 token 生成与登录状态保持

---

## 三、登录 / 注册功能（全链路完成）

### **注册功能**

* 创建 Register 页面并实现表单交互
* 成功与 `/auth/register` 后端路由对接
* 修复 500 错误（数据库字段与权限）
* 注册可正常写入数据库

### **登录功能**

* 完成 Login 页面 UI + 交互
* 成功对接 `/auth/login`
* 登录返回 token
* token 已成功写入 `localStorage`

---

## 四、创意模块实现（创作前）完成

* 完成了推荐页（RecommendFeed）的整体功能，实现了前端 Mock 推荐流数据结构（VideoItem + MOCK_VIDEOS，瀑布流布局（类似小红书），无限滚动（IntersectionObserver），自动加载更多视频、生成唯一 id
推荐卡片（RecommendCard）完成样式 + 动作跳转
* 解释与优化：

  * mock 数据中的 id
  * RecommendFeed 自动生成 id
  * `Array.from()` 的数据生成机制
* 清理数据重复叠加问题
* 首页瀑布流功能运行正常，滚动加载正常

## 五、创作模块实现（创作中/后）完成

* 实现了视频上传功能

* 实现了我的页面，成功显示上传视频 
---

## 六、当前存在的问题 / 待完成事项 TODO LIST

* 视频播放仍有问题(正常播放，全屏播放..)
* 后台可视化功能未开发
* 推荐视频数据仍使用 mock，待接入真实后端
* 美化、现代化前端
* 数据库工具引入
* 开源视频播放器使用
